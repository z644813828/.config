check program vpn_gateway with path "/etc/monit/scripts/vpn_gateway.sh"
with timeout 30 seconds
    if status > 0
        for 60 cycles then alert
    alert my_gmail with mail-format  {     # use local format
        subject: 🔐 VPN gateway state changed
    }
    with reminder on 2880 cycles
