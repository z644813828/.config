check program backups with path "/etc/monit/scripts/check-backups.sh"
with timeout 3600 seconds
    if status > 0 then alert
    alert my_gmail with mail-format  {     # use local format
        subject: 💾 Backups state changed
    }


